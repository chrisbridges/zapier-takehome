meta {
  name: "Not found: unknown customer"
  type: "http"
  seq: 6
}

post {
  url: {{baseUrl}}/usage
  body: json
}

headers {
  Content-Type: "application/json"
  Idempotency-Key: "{{idempotencyKey}}"
}

body:json {
  {
    "customerId": {{unknownCustomerId}},
    "service": "{{service}}",
    "unitsConsumed": "{{unitsConsumed}}",
    "pricePerUnit": "{{pricePerUnit}}"
  }
}
